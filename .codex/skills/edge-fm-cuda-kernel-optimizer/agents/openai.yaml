interface:
  display_name: "Edge-FM CUDA Optimizer"
  short_description: "Triage NSYS traces and optimize Edge-FM CUDA kernels"
  default_prompt: "Use $edge-fm-cuda-kernel-optimizer to triage Edge-FM NSYS traces, isolate hotspots, and optimize CUDA or CUTLASS kernels with NCU-guided iteration."
