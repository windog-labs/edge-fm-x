# H100 pi0.5 Native Session Passed

2026-09-09 05:10 UTC. A real no-Python pi0.5 full-model Session on H100 now
passes with explicit numerical-provider enforcement. This replaces the earlier
H100 `failed-numerics` status for the ATen AOTI path.

## Result

- Native PID 1856601, exit 0, Python not linked.
- `same_artifact_all_exact=true`, `official_all_exact=true`.
- Two complete normalized actions, each byte-exact against both the same ATen
  AOTI artifact and official reference.
- Numerical provider enforcement verified with an explicit exclusive-process
  and calling-thread worker bootstrap.

Remote report SHA: `073c6926e339b05b07291db42358f5389727157b9da4fc07be929bde6bdaf68a`.
Remote selection SHA: `8935c58c1ec81461434e9d0ee7d538e00d364acf2636b897d73aafe38cd69e3f`.
Bundle manifest SHA: `d962302a1c71637da69c93fd4476aa35909f6f6c997b3b5fafc83e8b08d5c9e0`.

## Public Fix

The generic OpenPI session adapter now accepts legacy compile reports that
contain one complete context at build-report level rather than per-Region
`observed_numerical_context_before/after`. A legacy `/1` context is projected
into the current enforceable `/2` policy without silent in-place upgrades.
`openpi_numerical.py` and `openpi_session.py` were extended and the legacy
binding regression tests pass (19 tests).

## Remaining

This is single-frame native validation, not formal three-policy replay/CDF.
The next step is reusing the same ATen artifacts with a benchmark variant
protocol to generate H100 pi0.5 formal replay evidence.
