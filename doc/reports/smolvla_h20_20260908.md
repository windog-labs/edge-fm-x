# SmolVLA H20 Follow-up

2026-09-08 11:47 UTC. **Formal experiment, remote audit, local raw-output audit
and CDF visual inspection completed.** The available-hardware Goal and full
paper acceptance remain incomplete.

Local source/evidence:
`artifacts/edgefm-vla-goal/20260906-044509/smolvla-h20-20260908-001`.

Remote:
`zzm-h20-x8-2:/xs-train-nas/zzm/repos/edgefm-vla-goal-20260906/runs/smolvla-h20-20260908-001`.

GPU0 UUID: `GPU-ae321531-504c-f416-e693-94fa02779130`.
Controller PID 3766345 and formal-stage PID 3775194 are historical launch IDs.
The pipeline exited successfully; do not restart the create-only run.

## Completed Gates

- Source: 292 implementation files frozen from the local dirty tree at
  `592fc320d57398571dcb4ab686d5fb55fe9bac86`; original 329-file setup manifest SHA
  `02a01df30f9fa62eb8ad88b7da907935ab983ed41078bdb6ce715dd75efc258f`.
  Current local compiler additions were not copied into this already frozen run.
- Official LeRobot revision `8fff0fde7c79f23a93d845d1a50e985de01f8b8a`,
  339 upstream source files, checkpoint/tokenizer assets and strict-statistics
  16-episode input pack independently checked on the H20 filesystem.
- Fresh same-host official references and four strict captures passed. All
  16 complete normalized action arrays F32[1,50,6], 4800 values, match exported IR
  byte-for-byte. Capture SHA:
  `33d417ac87c55ff24e9866edb9345153b6b6cd62ac9f83ce5f21445491aef63b`.
- Five TorchScript Regions, including the official inverse normalization, and
  new full-IR dual outputs passed on all 16 samples. Direct report SHA:
  `22c55fb678308cc53a1f98a018ed60161905195ab16ab95aa357de0ee543d5b8`.
- New off/batch-only/required C++ builds and pilots passed. Independent pilot
  audit checks 144 calls and 288 complete normalized/native tensors, including
  numerical worker configuration. SHA:
  `9a9004795b3014783daff065466e50fb50d240ede0ce1d825384d1052482cd66`.
  All nine tests on real pilot evidence passed before formal launch.

## Formal Protocol

Each of three policies uses five processes, 128 warmups and 1024 measured calls
per process. Full outputs are checked, not sampled action coordinates. Actual
replay counters, GPU owner registration and NVML telemetry are retained. The
boundary is resident CUDA model tensors through synchronized Session completion,
not camera-to-robot latency. Formal rows, CDF, raw-output audit and local archive
are now independently verified. The measured 15360 calls exclude warmups;
17280 total calls produce 34560 complete tensors / 10368000 values, all exact.

| Policy | Mean ms | p99 ms | Fresh chunks/s | Peak sampled GPU MiB |
| --- | ---: | ---: | ---: | ---: |
| off | 117.632460 | 136.829908 | 8.501055 | 2435 |
| batch-only | 118.379805 | 184.700100 | 8.447387 | 2439 |
| required | 47.112908 | 49.797612 | 21.225606 | 3213 |

The required policy reduces mean latency by 59.9490584% relative to off.
Batch-only does not improve aggregate mean latency in this run. Sampled device
memory increases by 778 MiB; this is not an allocator peak, memory saving or
Agent-kernel speedup claim. Every required worker records a real 10-step capture,
1152 replays and zero ordinary executions. All latency outliers are retained.

The GPU had exclusive registered ownership per worker. The host was not reserved
exclusively: this campaign overlapped the same project's CogACT compilation and
builds on another GPU, with shared CPU/memory/NAS resources. This observed CDF is
not a certified interference-free hardware limit; a quiet controlled repeat
belongs with the remaining same-platform/vendor comparisons. No CPU-contention
effect is inferred or subtracted from the recorded latencies.

## Evidence Closure

- Remote complete pipeline SHA:
  `b01f71dd53ed79c66cda98e1d35a150806ca451dc07b766a92132e375dbc5e48`.
- Remote independent formal audit SHA:
  `66a2f82f7669e6369206bb2d92af8c144485152e4a273b7330b8ee539d3807bb`.
- `formal-retrieval-001`: 648 files / 219335419 bytes; evidence only, not a full
  runnable model bundle. The runnable bundles remain in the isolated remote run.
- Local raw audit SHA:
  `84cb2bd3047fbb81a0bd01bcabfa39d3d91a1b3e906dc53524fe9135f8bb929c`.
  All raw bytes and MSE/max-abs checks remain exact. 11880 cosine/norm metadata
  entries differ by at most two ULP across CPUs, below the separately declared
  four-ULP metadata bound; these are not output-value differences.
- `formal-figures-001/complete-table.csv`, `complete-output-metrics.json` and
  `latency-cdf-tail.png` / `.pdf` contain the complete table and curves.
  The PNG was actually viewed, with readable labels and all tails visible.
- `formal-closeout-001.json` SHA:
  `c039235b976528a9d5ebb0e4087d1f467f18888386d1d32352433ba67f6eb77d`.

## Transport Record

Source rsync completed. Slow asset rsync PID 2008644 was explicitly terminated
after an exact-revision mirror route was demonstrated; its exit 20 receipt and
partial file are retained. Primary HF connectivity preflight failed, also kept.
The existing HF download API then fetched the same pinned revisions through
`hf-mirror.com`. Both large assets and all ten metadata/tokenizer files passed
exact local SHA-256 and size checks before setup. The mirror is transport only;
neither model revision nor source code was replaced.

H20 Python 3.11.16 and Torch 2.10.0+cu128 are recorded independently of H100's
environment. H100 results are not reused as same-host reference execution or
native binaries. Recovered robot statistics, saved noise, prepared-tensor
boundary and missing physical robot calibration remain explicit limitations.

## Resume

This run is complete and should not be relaunched. Any expanded workload or
changed implementation belongs in a new run directory. For an independent
repeat of the local audit, use a new output filename with
`verify_retrieved_execution_layout_v2.py`, `--execution-subdir complete-001`,
the frozen local source, `--setup-manifest local-setup.json`,
`--audit-script audit_complete_session.py`, and `--cross-host-metrics`.
Use the hashes above to verify the original completed retrieval and audits.
