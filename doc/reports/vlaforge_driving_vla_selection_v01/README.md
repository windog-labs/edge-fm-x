# Driving VLA Candidate Selection v01

This report records the bounded candidate audit that selected MindDrive 0.5B
for the next real driving-VLA evidence chain. It deliberately makes only an
L0 provenance claim.

Authoritative machine-readable data:
[`candidate_selection.json`](candidate_selection.json).

The report distinguishes public metadata from locally verified execution.
Checkpoint LFS hashes and sizes are pinned before download; the Model
Adaptation Card remains `L0-selected` until strict local load and a real
six-camera eager run pass.
