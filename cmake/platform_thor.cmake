# ============================================================================
# Platform Configuration: NVIDIA Thor
# ============================================================================
# TODO: Add platform-specific configuration for NVIDIA Thor

set(PLATFORM_NAME "NVIDIA Thor")
set(PLATFORM_ARCH "")
set(PLATFORM_GPU "Thor")

message(STATUS "Platform: ${PLATFORM_NAME}")
message(STATUS "  GPU: ${PLATFORM_GPU}")
message(WARNING "Platform configuration for ${PLATFORM_NAME} is not yet implemented")

